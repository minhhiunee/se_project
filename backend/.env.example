# Copy this file to .env and replace USER, PASSWORD, and DB name if needed.
# Special characters in PASSWORD must be URL-encoded (e.g. @ -> %40).
# Create the database first: CREATE DATABASE se_project;
DATABASE_URL="mysql://USER:Minhhieu06%40@localhost:3306/se_project"

# Used to sign and verify JWT access tokens (use a long random string in production).
JWT_SECRET="supersecretkey"
